{
  "name": "renaissance-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "wrangler dev --port 8787 & sleep 5 && node test-mcp.js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
